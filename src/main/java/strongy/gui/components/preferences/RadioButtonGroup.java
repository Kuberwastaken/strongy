package strongy.gui.components.preferences;

public class RadioButtonGroup {
	// This was a Swing logical group; with JavaFX, we use ToggleGroup directly
	// inside the RadioButtonPanel instead. This class is no longer strictly needed
	// but
	// kept empty to resolve any remaining compile-time dependencies that just
	// declare it.
}