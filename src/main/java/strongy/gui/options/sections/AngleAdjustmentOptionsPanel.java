package strongy.gui.options.sections;

import javafx.geometry.Pos;
import strongy.gui.components.panels.ThemedPanel;
import strongy.gui.style.StyleManager;
import strongy.io.preferences.StrongyPreferences;

public class AngleAdjustmentOptionsPanel extends ThemedPanel {
	public AngleAdjustmentOptionsPanel(StyleManager styleManager, StrongyPreferences preferences) {
		super(styleManager);
		setSpacing(8);
		setAlignment(Pos.TOP_LEFT);
		// Stub
	}
}
